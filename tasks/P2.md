# P2: attestation index
Deliver:
- SQLite day-shard writer module (WAL), row: attestationId, decisionId, agentId, schemaHash, decisionHash, prev, ts, leafType, bundlePath
- Backfill script over a bundle-store directory
- Query lib: byDecisionId, byAgentRange, byAgentSchema, chainHead
Accept: all four queries <10 ms on a 1M-row fixture shard, backfill idempotent, index rebuildable from bundles alone
