# P3: adapters
Deliver:
- packages/jev: stub client emitting Jev's documented decision shape, one mapping file, toDecision() -> DAR inputs
- packages/schema: raw JSON Schema + Zod (via zod-to-json-schema) -> toDecision()
Accept: stub end-to-end test produces valid DARs through the P1 batcher against a mock transport, all Jev shape knowledge confined to the mapping file, zod conversion vectors pass
