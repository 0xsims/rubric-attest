# P1: core SDK (packages/attest-decision)
Deliver:
- JCS canonicalization + SHA3-256 hashing module
- DAR builder per spec/dar-0.1.md, ULID decisionId, prev chaining per agentId
- attest(): fire-and-forget, enqueue, never throws to caller
- Batcher: flush at 64 records or 5000 ms; one POST per flush to /v1/tiered-attest
- Durable spool: append-only file, drain on recovery, 50 MB cap drop-oldest
Accept: JCS cross-vectors pass, DAR golden records match, kill -9 during flush loses zero spooled records, attest() adds <1 ms to caller
