# P5: evidence export CLI
Deliver:
- rubric-evidence export --agent --from --to: JSON bundle of DARs, proofs, anchor refs, continuity report, schema-change log, plus plain-text summary
Accept: golden bundle matches fixture index+store, gap and fork cases render correctly in the continuity report, runs offline against index shards
