{
  "name": "rubric-attest",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "description": "Rubric Decision Attestation — monorepo (DAR/0.1)",
  "license": "Apache-2.0",
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "lint": "eslint .",
    "typecheck": "tsc -p tsconfig.json",
    "build": "tsc -p packages/attest-decision/tsconfig.build.json && tsc -p packages/attest-index/tsconfig.build.json && tsc -p packages/jev/tsconfig.build.json && tsc -p packages/schema/tsconfig.build.json && tsc -p packages/verify/tsconfig.build.json && tsc -p packages/evidence/tsconfig.build.json",
    "clean": "rm -rf packages/*/dist packages/*/*.tsbuildinfo",
    "test": "vitest run",
    "test:perf": "vitest run --config vitest.perf.config.ts",
    "check": "npm run lint && npm run typecheck && npm run test"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/better-sqlite3": "^7.6.11",
    "@types/node": "^20.16.11",
    "eslint": "^9.15.0",
    "globals": "^15.12.0",
    "tsx": "^4.20.6",
    "typescript": "^5.6.3",
    "typescript-eslint": "^8.15.0",
    "vitest": "^4.1.11"
  }
}
