/**
 * @rubric/jev — Jev decision-shape adapter.
 *
 * P0 scaffolding. All knowledge of Jev's documented decision shape is confined
 * to this package's mapping file (added in P3, see tasks/P3.md), which exposes
 * `toDecision()` producing DAR inputs for @rubric/attest-decision.
 */

/** Placeholder until the P3 mapping + toDecision() land. */
export const JEV_ADAPTER_VERSION = "0.1.0" as const;
