/**
 * @rubric/schema — JSON Schema / Zod adapter.
 *
 * P0 scaffolding. In P3 this exposes raw JSON Schema and Zod
 * (via zod-to-json-schema) paths to `toDecision()` producing DAR inputs for
 * @rubric/attest-decision (see tasks/P3.md).
 */

/** Placeholder until the P3 conversion + toDecision() land. */
export const SCHEMA_ADAPTER_VERSION = "0.1.0" as const;
